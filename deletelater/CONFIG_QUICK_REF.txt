```
╔══════════════════════════════════════════════════════════════════════╗
║                   SIMPLE CONFIGURATION SYSTEM                        ║
║                    ✅ IMPLEMENTATION COMPLETE                        ║
╚══════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────┐
│ YOUR WORKFLOW (3 Simple Steps)                                       │
└──────────────────────────────────────────────────────────────────────┘

  1️⃣  Edit config.yaml
      ↓
      vim config.yaml
      (Change catalog, warehouse, llm, etc.)

  2️⃣  Generate app.yaml
      ↓
      python generate_app_yaml.py dev
      (Auto-creates app/app.yaml with env vars)

  3️⃣  Deploy
      ↓
      databricks bundle deploy
      (App uses environment variables automatically!)


┌──────────────────────────────────────────────────────────────────────┐
│ DATA FLOW                                                            │
└──────────────────────────────────────────────────────────────────────┘

    config.yaml                    You edit this once
         │
         ├─────────────┬─────────────────┐
         │             │                 │
         ▼             ▼                 ▼
  generate_app_   shared/config   databricks.yml
     yaml.py          .py          (manual)
         │             │                 │
         │             ▼                 │
         │        Notebooks              │
         │      (use config.py)          │
         │                               │
         ▼                               │
   app/app.yaml ───────────────────────┐ │
  (env vars)                           │ │
         │                             │ │
         ▼                             ▼ ▼
  Databricks Apps                  Deployment
   (sets env)                    (DAB bundles)
         │
         ▼
    Streamlit Code
  import os
  catalog = os.getenv("CATALOG_NAME")
         │
         ▼
    ✅ WORKS!


┌──────────────────────────────────────────────────────────────────────┐
│ WHAT'S IN EACH FILE                                                  │
└──────────────────────────────────────────────────────────────────────┘

  📄 config.yaml (YOU EDIT)
      environments:
        dev:
          catalog: "fraud_detection_dev"
          warehouse_id: "148ccb90800933a1"
          llm_endpoint: "databricks-claude-sonnet-4-5"

  🤖 generate_app_yaml.py (GENERATOR)
      Reads config.yaml
      Generates app/app.yaml

  📋 app/app.yaml (AUTO-GENERATED)
      env:
        - name: 'CATALOG_NAME'
          value: 'fraud_detection_dev'
        - name: 'DATABRICKS_WAREHOUSE_ID'
          value: '148ccb90800933a1'

  💻 Your Code (SIMPLE)
      import os
      CATALOG = os.getenv("CATALOG_NAME")


┌──────────────────────────────────────────────────────────────────────┐
│ USAGE EXAMPLES                                                       │
└──────────────────────────────────────────────────────────────────────┘

  📌 In Main App (app_databricks.py):
      import os
      CATALOG = os.getenv("CATALOG_NAME", "fraud_detection_dev")
      SCHEMA = os.getenv("SCHEMA_NAME", "claims_analysis")
      st.write(f"Catalog: {CATALOG}")

  📌 In Pages (pages/1_claim_analysis.py):
      import os
      CATALOG = os.getenv("CATALOG_NAME")
      WAREHOUSE_ID = os.getenv("DATABRICKS_WAREHOUSE_ID")
      # Use them!

  📌 In Agent (utils/fraud_agent.py):
      import os
      llm_endpoint = os.getenv("LLM_ENDPOINT")
      catalog = os.getenv("CATALOG_NAME")
      schema = os.getenv("SCHEMA_NAME")


┌──────────────────────────────────────────────────────────────────────┐
│ AVAILABLE ENVIRONMENT VARIABLES                                      │
└──────────────────────────────────────────────────────────────────────┘

  ✓ DATABRICKS_HOST            - Workspace URL
  ✓ DATABRICKS_WAREHOUSE_ID    - SQL Warehouse ID
  ✓ CATALOG_NAME               - Unity Catalog name
  ✓ SCHEMA_NAME                - Schema name
  ✓ VECTOR_ENDPOINT            - Vector Search endpoint
  ✓ LLM_ENDPOINT               - LLM model endpoint
  ✓ ENVIRONMENT                - Environment (dev/staging/prod)
  ✓ EMBEDDING_MODEL            - Embedding model name


┌──────────────────────────────────────────────────────────────────────┐
│ QUICK COMMANDS                                                       │
└──────────────────────────────────────────────────────────────────────┘

  🚀 Generate + Deploy:
      ./deploy_with_config.sh dev

  🔧 Manual Steps:
      python generate_app_yaml.py dev
      databricks bundle deploy

  🔍 Check Generated File:
      cat app/app.yaml

  📝 Edit Config:
      vim config.yaml


┌──────────────────────────────────────────────────────────────────────┐
│ ENVIRONMENT SWITCHING                                                │
└──────────────────────────────────────────────────────────────────────┘

  Dev Environment:
      python generate_app_yaml.py dev
      → Uses: fraud_detection_dev

  Staging Environment:
      python generate_app_yaml.py staging
      → Uses: fraud_detection_staging

  Prod Environment:
      python generate_app_yaml.py prod
      → Uses: fraud_detection_prod


┌──────────────────────────────────────────────────────────────────────┐
│ TWO CONFIG PATTERNS IN THIS PROJECT                                  │
└──────────────────────────────────────────────────────────────────────┘

  Pattern 1: Notebooks (Filesystem)
      from shared.config import get_config
      cfg = get_config()  # Reads config.yaml from disk

  Pattern 2: Streamlit App (Environment Variables)
      import os
      catalog = os.getenv("CATALOG_NAME")  # Reads env var


┌──────────────────────────────────────────────────────────────────────┐
│ FILES CREATED                                                        │
└──────────────────────────────────────────────────────────────────────┘

  ✅ generate_app_yaml.py        - Generator script
  ✅ app/app.yaml                 - Auto-generated env vars
  ✅ deploy_with_config.sh        - One-command deployment
  ✅ CONFIG_SIMPLE.md             - Simple architecture guide
  ✅ CONFIG_SUMMARY.md            - Quick summary
  ✅ CONFIG_FLOW_VISUAL.md        - Visual diagrams
  ✅ This file                    - Quick reference


┌──────────────────────────────────────────────────────────────────────┐
│ WHY THIS IS SIMPLE                                                   │
└──────────────────────────────────────────────────────────────────────┘

  ✓ No complex config classes
  ✓ No imports needed (just os)
  ✓ Standard Python os.getenv()
  ✓ One file to edit (config.yaml)
  ✓ Auto-generates app.yaml
  ✓ Works everywhere


┌──────────────────────────────────────────────────────────────────────┐
│ SUMMARY                                                              │
└──────────────────────────────────────────────────────────────────────┘

  YOU EDIT:     config.yaml
  SCRIPT RUNS:  python generate_app_yaml.py dev
  FILE CREATED: app/app.yaml (with env vars)
  CODE USES:    os.getenv("CATALOG_NAME")

  ➜ Single source of truth
  ➜ Simple code
  ➜ Everything in app.yaml
  ➜ Just works! ✅


╔══════════════════════════════════════════════════════════════════════╗
║  See CONFIG_SIMPLE.md for complete documentation                     ║
╚══════════════════════════════════════════════════════════════════════╝
```

